apiVersion: v1
kind: Service
metadata:
  name: django-app-service
  namespace: messaging-app
spec:
  selector:
    app: django
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 8081        
      targetPort: 8081  
